function itemNew(){
    let val = 'https://shareshortcuts.com/download/1832-ip-lookup.html';
const selBox = document.createElement('textarea');
selBox.value = val;
document.body.appendChild(selBox);
selBox.focus();
selBox.select();
document.execCommand('copy');
document.body.removeChild(selBox);
alert('url copied to clipboard');
}
function item1(){
    let val = 'https://shareshortcuts.com/download/1283-apps-configuration.html';
const selBox = document.createElement('textarea');
selBox.value = val;
document.body.appendChild(selBox);
selBox.focus();
selBox.select();
document.execCommand('copy');
document.body.removeChild(selBox);
alert('url copied to clipboard');
}
function item2(){
    let val = 'https://shareshortcuts.com/download/1542-myip.html';
const selBox = document.createElement('textarea');
selBox.value = val;
document.body.appendChild(selBox);
selBox.focus();
selBox.select();
document.execCommand('copy');
document.body.removeChild(selBox);
alert('url copied to clipboard');
}
function item3(){
    let val = 'https://shareshortcuts.com/download/1503-robodown.html';
const selBox = document.createElement('textarea');
selBox.value = val;
document.body.appendChild(selBox);
selBox.focus();
selBox.select();
document.execCommand('copy');
document.body.removeChild(selBox);
alert('url copied to clipboard');
}
function item4(){
    let val = 'https://shareshortcuts.com/download/1500-robo-shorten.html';
const selBox = document.createElement('textarea');
selBox.value = val;
document.body.appendChild(selBox);
selBox.focus();
selBox.select();
document.execCommand('copy');
document.body.removeChild(selBox);
alert('url copied to clipboard');
}
function item5(){
    let val = 'https://shareshortcuts.com/download/1474-website-translator.html';
const selBox = document.createElement('textarea');
selBox.value = val;
document.body.appendChild(selBox);
selBox.focus();
selBox.select();
document.execCommand('copy');
document.body.removeChild(selBox);
alert('url copied to clipboard');
}
function item6(){
    let val = 'https://shareshortcuts.com/download/1469-terminal.html';
const selBox = document.createElement('textarea');
selBox.value = val;
document.body.appendChild(selBox);
selBox.focus();
selBox.select();
document.execCommand('copy');
document.body.removeChild(selBox);
alert('url copied to clipboard');
}
function item7(){
    let val = 'https://shareshortcuts.com/download/1285-robopass.html';
const selBox = document.createElement('textarea');
selBox.value = val;
document.body.appendChild(selBox);
selBox.focus();
selBox.select();
document.execCommand('copy');
document.body.removeChild(selBox);
alert('url copied to clipboard');
}
function item8(){
    let val = 'https://shareshortcuts.com/download/1351-safari.html';
const selBox = document.createElement('textarea');
selBox.value = val;
document.body.appendChild(selBox);
selBox.focus();
selBox.select();
document.execCommand('copy');
document.body.removeChild(selBox);
alert('url copied to clipboard');
}